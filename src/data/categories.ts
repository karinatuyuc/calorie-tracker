//Importando types de category
import type { Category } from "../types"

export const categories: Category[] = [
    { id: 1, name: 'Comida'},
    { id: 2, name: 'Ejercicio'} //It's not recognized the id, but let's see if does now
]
